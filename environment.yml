name: climate_env
channels:
  - conda-forge
  - defaults
dependencies:
  - birdy
  - gdal>=3.4.1
  - matplotlib
  - pip
  - python>=3.8.16
  - xarray
  - pip:
    - cdsapi
    - geojson
    - netcdf4
    - numba
    - scipy
    - sentinelsat
prefix: ~/anaconda3/envs/climate_env
